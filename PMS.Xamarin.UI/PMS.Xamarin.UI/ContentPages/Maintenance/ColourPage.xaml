<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PMS.Xam.UI.ContentPages.Maintenance.ColourPage">
  <Label Text="{Binding MainText}" VerticalOptions="Center" HorizontalOptions="Center" />
  
  <ContentPage.Content>
    <StackLayout VerticalOptions="FillAndExpand"
                 HorizontalOptions="FillAndExpand"
                 Orientation="Vertical"
                 Spacing="15">
      <ListView x:Name="ColourListView"
              VerticalOptions="FillAndExpand"
              HorizontalOptions="FillAndExpand"
              IsPullToRefreshEnabled="True"
              IsEnabled="True"
                 >
        <ListView.ItemTemplate>
          <DataTemplate>
            <TextCell Text="{Binding Name}" TextColor="{Binding Code}" ></TextCell>
            <MenuItem Clicked="OnEdit" CommandParameter="{Binding .}" Text="Edit" ></MenuItem>
            <MenuItem Clicked="OnDelete" CommandParameter="{Binding .}" IsDestructive="True" Text="Delete"></MenuItem>

          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
      
    </StackLayout>
  </ContentPage.Content>
  
</ContentPage>